[project]
name = "deleteme-model-oilgas"
version = "0.1.0"
description = "Temporary placeholder oil & gas data models"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/deleteme_model_oilgas"]
